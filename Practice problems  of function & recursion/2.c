//write a function to find square root of a numbers
#include<stdio.h>
#include<math.h>
 int root(int n)
{
    return sqrt(n);

}

int main()
{
    int n=25;
    printf("Root of the number is :%d\n", root(n));
}
